[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tridentchain-security"
version = "0.1.3"
description = "TridentChain Security — local-first supply chain vulnerability scanner for project, system, and extensions."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [
  { name = "TridentChain Security Contributors" }
]
dependencies = [
  "requests>=2.31.0",
  "defusedxml>=0.7.1"
]
keywords = ["security", "supply-chain", "vulnerability", "scanner", "sbom"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Developers",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Security",
  "Topic :: Software Development :: Quality Assurance"
]

[project.urls]
Homepage = "https://github.com/DevInder1/supply-chain-scanner-public"
Repository = "https://github.com/DevInder1/supply-chain-scanner-public"

[project.scripts]
tridentchain-security = "scanner.main:main"

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
include = ["scanner*"]

[tool.setuptools.package-data]
scanner = ["config.yaml"]
